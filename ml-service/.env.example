# Keycloak - obtained from Track A's Keycloak realm config
KEYCLOAK_ISSUER=https://auth.internal/realms/airport
KEYCLOAK_AUDIENCE=ml-service

# Shared Postgres (same DB as the backend, different least-privilege role)
DATABASE_URL=postgresql://ml_service_ro:changeme@postgres:5432/airport_tracking

# Comma-separated list of allowed origins - the deployed frontend URL(s) only
ALLOWED_ORIGINS=https://dashboard.internal
MODEL_DIR=/data/models

# Backend API - for PATCH /checkpoints/{id}/match and POST /api/v1/anomalies/detect
BACKEND_BASE_URL=https://api.internal

# Dedicated Keycloak client for the tag_offline background sweep (client_credentials
# grant - no inbound request to forward a user JWT from). Pending Shashank setting up
# the 'ml-service' client with Service accounts roles enabled.
ML_SERVICE_CLIENT_ID=ml-service
ML_SERVICE_CLIENT_SECRET=changeme
